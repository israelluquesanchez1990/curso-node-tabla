const { crearArchivo } = require('./helpers/multiplicar');  //Desestructuración

console.clear();

console.log(process.argv);
// Se accedes desde proces.argv, pero ya existe un paquete en npm para facilitarnos el acceso  'Yars'